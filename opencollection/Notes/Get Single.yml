info:
  name: Get Single
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/v1/notes/:id"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
