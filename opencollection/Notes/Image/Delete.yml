info:
  name: Delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/v1/notes/:id/images"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
