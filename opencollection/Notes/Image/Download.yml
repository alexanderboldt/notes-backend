info:
  name: Download
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/v1/notes/:id/images"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
