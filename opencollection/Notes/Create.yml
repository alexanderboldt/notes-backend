info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/v1/notes"
  body:
    type: json
    data: |-
      {
        "title": "Essen kochen",
        "description": "Soll lecker sein"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
