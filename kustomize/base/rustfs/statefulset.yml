apiVersion: v1
kind: Service
metadata:
  name: notes-rustfs
spec:
  type: NodePort
  selector:
    app: notes-rustfs
  ports:
    - name: api
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
      nodePort: 30200
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: notes-rustfs
spec:
  selector:
    matchLabels:
      app: notes-rustfs
  serviceName: notes-rustfs
  replicas: 1
  template:
    metadata:
      labels:
        app: notes-rustfs
    spec:
      containers:
        - name: notes-rustfs
          image: rustfs/rustfs:latest
          imagePullPolicy: IfNotPresent
          args:
            - server
            - /data
            - --console-address=:9001
          envFrom:
            - secretRef:
                name: notes-rustfs-secret
          ports:
            - name: api
              containerPort: 9000
            - name: console
              containerPort: 9001
          volumeMounts:
            - name: notes-rustfs-storage
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: notes-rustfs-storage
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 1Gi
